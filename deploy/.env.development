# Local development environment variables for Tonalogy Interface
# Copy this to .env.local in the root directory for local development

# Local API URL (assumes tonalogy-api running on port 8000)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Internal proxy URL for Next.js API routes (development only)
TONALOGY_API_URL=http://localhost:8000

# Development environment
NODE_ENV=development
